account_size: 661.0
daily_loss_kill_pct: 0.20
daily_loss_prekill_pct: 0.18
max_trades_per_session: 8
max_open_positions: 1
cooldown_seconds: 600
max_consecutive_losses: 2
commission_per_side: 2.50
slippage_ticks: 1
large_loss_threshold: 50.0        # Losses >= $50 trigger extended cooldown
large_loss_cooldown_seconds: 600  # 10 min cooldown after large loss
